# Install the apt prerequisites
# addons:
#   apt:
#     packages:
#       - python-pygments

language: go

go:
  - master

sudo: false

git:
  depth: 10

branches:
  only:
    - master

# env:
#   global:
#     - GIT_NAME="Deployer"
#     - GIT_EMAIL="deployer@frichet.org"
#     - SOURCE_DIR="public"
#     - DEPLOY_BRANCH="source"

cache:
  directories:
    - $GOPATH/src
    - $GOPATH/pkg

before_script:
  - go get github.com/spf13/hugo
  # # Clean and don't fail
  # - rm -rf public || exit 0

# Build the website (see scripts/gh-pages.sh)
script:
  - hugo # -d public

after_success:
  - echo "michel.frichet.org" > ./public/CNAME
  # - ./scripts/deploy.sh

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  local_dir: public
  # target_branch: gh-pages
  fqdn: michel.frichet.org
  email: deploy@frichet.org
  name: Deployer
  on:
    branch: master
